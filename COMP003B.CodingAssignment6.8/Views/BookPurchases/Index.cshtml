@model IEnumerable<COMP003B.CodingAssignment6._8.Models.BookPurchase>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Book)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Book.Author)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BookPurchaseId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BookPurchaseId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BookPurchaseId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
